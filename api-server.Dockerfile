# `<VERSION>`の部分をバージョン名（例: v0.6.1-beta.1）などに置き換えてください。
# 例: FROM kizahasi/flocon-api:v0.6.1-beta.1
# バージョン名は https://hub.docker.com/repository/docker/kizahasi/flocon-api から参照できます。
FROM kizahasi/flocon-api:<VERSION>